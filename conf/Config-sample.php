<?php

class Config {
  const dbDriver = 'mysql';
  const dbHost = 'localhost';
  const dbName = 'kanban';
  const dbUser = 'DBUSER';
  const dbPassword = 'DBPASSWORD';
  const dbDebug = true;
  const dbCharset = 'utf8';
  const restDebug = true;
  const ssoDebug = true;
  const ssoUrl = 'http://SSOHOST/sso.php';
}

?>
